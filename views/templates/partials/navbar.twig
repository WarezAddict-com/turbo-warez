{# Navbar #}

<nav class="navbar navbar-expand-sm navbar-dark fixed-top bg-dark shadow" id="topNav">
    <div class="container">

        <a class="navbar-brand fw-bold" href="{{ BaseUrl }}">{{ AppName }}</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle Navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="container w-100"></div>

        <div class="collapse navbar-collapse" id="navbarCollapse">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link fw-bold" href="{{ BaseUrl }}/login">Login</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link fw-bold" href="{{ BaseUrl }}/register">Register</a>
                </li>
            </ul>
        </div>

    </div>
</nav>
